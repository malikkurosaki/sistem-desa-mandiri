import { pagePath } from "@/lib/pagePath";
import { Splash } from "@/ui/splash/Spash";
import { Stack } from "@mantine/core";

export default async function Page() {

    return <Stack>
        <Splash />
    </Stack>
}
