export interface IDataPosition {
    name: string;
    idGroup: string;
    group: string;
    id: string;
    isActive: boolean
};

export interface IStatusPosition {
    isActive: boolean
}

export interface IFormPosition {
    name: string
    idGroup: string
}